import dotenv from 'dotenv'

// Initialize all environment variables to appear in the process.env
dotenv.config()

// Alignment for logging
console.log('\n\n')

import './core/index'

export default ''
